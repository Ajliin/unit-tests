export const findIndex = (array, value) => {
  
  // //const items = ['Apple', 'Banana', 'Orange', 'Pear', 'Lemon']
  
  // if (array[value]){
  // const index = items.findIndex(index => index === 'Apple')
  // return index
  // }else if (items[4]) {
  //   const index = items.findIndex(index => index === 'Lemon')
  // }
  return array.indexOf(value)

  }

